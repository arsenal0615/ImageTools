---
description: 所有开发在 git worktree 中完成，任务结束后提交并推送
alwaysApply: true
---

# Git Worktree 开发流程

所有开发任务必须在 git worktree 中完成，禁止直接在主仓库（main 分支）上修改代码。

## 开始任务前

1. 根据任务内容确定一个简短的分支名，格式为 `feat/xxx`、`fix/xxx` 或 `chore/xxx`
2. 在项目根目录的同级创建 worktree：
   ```
   git worktree add ../ImageTools-<分支名> -b <分支名>
   ```
3. 切换工作目录到新建的 worktree 中进行开发

## 开发过程中

- 所有文件修改、新增、删除操作都在 worktree 目录中进行
- 如果 worktree 中缺少 `node_modules`，需要先运行 `npm install`

## 任务完成后

1. 在 worktree 目录中运行 `git status` 查看所有变更文件
2. 运行 `git diff` 查看具体改动内容
3. 使用 `git add` 将相关变更文件加入暂存区（只添加与本次任务相关的文件）
4. 用简洁的中文提交信息执行 `git commit`，提交信息应说明"为什么"而非"做了什么"
5. 执行 `git push -u origin <分支名>` 推送到远程
6. 运行 `git status` 确认提交和推送成功

## 注意事项

- 不要提交包含敏感信息的文件（如 `.env`、`credentials.json` 等）
- 如果 `git push` 失败（如远程有新提交），先执行 `git pull --rebase` 再重试 push
- 如果没有实际变更（没有修改文件），不要创建空提交
- 提交信息使用中文，保持简洁（1-2 句话）
- 如果任务只是回答问题或查看代码而没有修改文件，则跳过整个流程
- 任务完成后不要自动删除 worktree，由用户决定是否清理
