---
description: 完成任务后自动提交 git 变更
alwaysApply: true
---

# 任务完成后提交 Git

当你完成用户请求的编码任务后，必须执行以下 git 提交流程：

## 提交流程

1. 运行 `git status` 查看所有变更文件
2. 运行 `git diff` 查看具体改动内容
3. 使用 `git add` 将相关变更文件加入暂存区（只添加与本次任务相关的文件）
4. 用简洁的中文提交信息执行 `git commit`，提交信息应说明"为什么"而非"做了什么"
5. 提交完成后运行 `git status` 确认提交成功

## 注意事项

- 不要提交包含敏感信息的文件（如 `.env`、`credentials.json` 等）
- 不要执行 `git push`，除非用户明确要求
- 如果没有实际变更（没有修改文件），不要创建空提交
- 提交信息使用中文，保持简洁（1-2 句话）
- 如果任务只是回答问题或查看代码而没有修改文件，则跳过提交步骤
